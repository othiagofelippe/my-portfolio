# STATE — my-portfolio

## Decisions

| ID     | Decision                                                                                                                                                                                                                                 | Status | Date       | Rationale                                       |
| ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------- | ----------------------------------------------- |
| AD-P01 | O trabalho lado-portfolio do tf.ds v2 é rastreado aqui (`tfds-v2-migration`), espelhando as tasks T25–T27/T31–T32 da feature `tfds-v2` do repo tf.ds; a spec e os requirement IDs (MIG-\*, ANA-06) vivem lá — este repo não duplica spec | active | 2026-07-12 | Uma fonte de requisitos; dois repos de execução |

## Handoff

- **Feature `next16-upgrade` — T1–T3, T5–T7 concluídos (2026-07-19/20); T4 bloqueada.** PRs #16 (T1+T2), #17 (T1+T2→main), #25 (T5+T6→develop), #26 (develop→main). Produção em `8acc684` READY na Vercel (`othiagofelippe.com`), validado por curl + título de página.
  - **T1+T2**: `ci.yml` com Node 22, format-check, concurrency; `dependabot.yml` (npm+github-actions, semanal). Dependabot já abriu PRs #18–24, #27 nas horas seguintes (bumps automáticos, incluindo majors) — revisar antes de mergear, alguns (eslint 10, @types/node 26) são majors arriscados.
  - **T3**: secret scanning + push protection, vulnerability alerts + automated security fixes (pré-requisito não documentado na task original — vulnerability alerts precisa estar ligado antes), delete-branch-on-merge, branch protection na `main` exigindo `quality-check` verde.
  - **T4 (DNS Hostinger) BLOQUEADA**: `DNS_deleteDNSRecordsV1` do MCP `hostinger-mcp` rejeita o parâmetro `filters` em qualquer formato (array de objetos, array de arrays, objeto único — testados 6 formatos) com "must be an array", mesmo passando um array de fato. Parece bug de serialização no wrapper MCP. `DNS_updateDNSRecordsV1` não serve de alternativa (exige `records` não-vazio, não deleta). Registros a remover continuam pendentes: AAAA `@` (`2a02:4780:13:1299:0:2266:77d2:2`) e A `ftp` (`185.245.180.98`) em `othiagofelippe.com`. Snapshot de rollback confirmado (`144487976`, 2026-05-03) e renovação automática do domínio já ativa — nada bloqueado por aí. Resolver manualmente no painel Hostinger ou re-tentar se o tool for corrigido.
  - **T5**: Next 15.5.20→16.2.10, React 19.1.2→19.2.7 via `@next/codemod upgrade latest`. `middleware.ts`→`proxy.ts` (export `proxy`), `--turbopack` removido dos scripts (default no Next 16). Achados não previstos na task: (1) `eslint-config-next@16` agora exporta flat config nativo — o `FlatCompat`/`compat.extends('next/core-web-vitals', 'next/typescript')` antigo quebrava com `TypeError: Converting circular structure to JSON` (conflito com `eslint-plugin-react-hooks@7`); migrado para `import nextConfig from 'eslint-config-next'` direto. (2) `eslint-plugin-react-hooks@7` traz a regra nova `react-hooks/set-state-in-effect`, que passou a barrar o padrão clássico de "mounted flag" usado pra renderização segura de hidratação (Logo, ThemeToggle) — resolvido com hook `useMounted` via `useSyncExternalStore`; o caso do `AudioContext` (sync único do localStorage no mount) foi suprimido pontualmente por comentário, não é o mesmo padrão. (3) `next build` do Next 16 não imprime mais a tabela de First Load JS por rota — First Load JS de `/[lang]` era 216 kB antes (registrado em 2026-07-19); não há como medir do jeito antigo agora, considerar `@next/bundle-analyzer` se o número for necessário de novo.
  - **T6**: `reactCompiler: true` em `next.config.ts` + `babel-plugin-react-compiler` instalado como devDependency (não vem por padrão). Build e smoke test (Playwright: 3 temas, scroll/animação Motion na timeline de Experience) sem erros de console — não precisou reverter.
  - **T7**: preview de `develop` e produção de `main` validados READY via MCP da Vercel + `curl`/título de página. `@eslint/eslintrc` removido do `package.json` (ficou sem uso após a migração do eslint config).
- **PT4 do tfds-v2 encerrado (2026-07-19):** auditoria via MCP da Vercel confirmou deploys READY de `main` (produção) e `develop` (preview) no commit `e9e827f` pós-migração — o checkpoint do usuário está cumprido. T7 do next16-upgrade formaliza isso na doc.
- **Feature anterior:** `tfds-v2-migration` — Phase 1 (PT1–PT4) em andamento. **PT1, PT2 e PT3 concluídos nesta sessão** (2026-07-12) na branch `feat/tfds-v2-migration` (criada a partir de `develop`, que estava 39 commits à frente do origin e com trabalho não commitado em 3 componentes — commitado à parte antes de abrir a branch, `ec2e9ad`).
- **PT1 (`ddb8708`)**: deps trocadas de `file:` para npm (`@tfds/react|tokens|icons@^0.0.1`); `@source` do Tailwind apontando para `node_modules/@tfds/react/dist` (pacote publicado só expõe `dist`, sem `src`); todos os imports `@tfds/components`→`@tfds/react` renomeados (14 arquivos). Descoberta não antecipada pela task: `Typography`/`Badge`/`Button` do DS não aceitam mais `className` (AD-007 do tf.ds — só primitivos de layout mantêm), o que quebrou o type-check em 43 usos espalhados por 9 arquivos de seções. Corrigido caso a caso: classes de espaçamento movidas para um `div` wrapper; overrides de cor/line-height/font-weight que precisavam recair no próprio elemento do DS foram feitos sobrescrevendo a CSS custom property específica que a variante do componente lê (ex.: `[--tfds-color-text-primary:var(--tfds-color-action-primary)]` num wrapper, escopado ao subtree — não vaza pra fora); combinações ícone+texto que dependiam do próprio `Typography` ser flex container foram reestruturadas movendo o ícone pra fora, num wrapper flex.
- **PT2 (`913d15c`)**: `Card` local (shadcn-style, com `CardHeader`/`CardContent`) substituído pelo `Card` do `@tfds/react` em `Experience.tsx` e `Skills.tsx` — Card do DS não tem subcomponentes nem `className`, então header/content foram recriados com `div`s simples. **Decisão do usuário**: o hover de sombra (`hover:shadow-lg` + `transition-shadow`) do Card local foi descartado — a API do Card do DS não tem variante de hover nem `className` pra adicionar; o hover lift via Motion (`whileHover={{ y: -3 }}`) foi mantido por ser independente do `className` do Card. Card local removido (ficou sem uso). `Grid` do DS (aceita `className`, ao contrário de Card/Typography/Badge) adotado em 6 grids simples (`cols`+`gap` cobre): Experience, Contact, Blog (ambas as páginas), Skills, stats do Hero. Grids com `grid-template-columns` exóticas (Projects, About) mantidos como estavam — fora do que `Grid.cols` cobre.
- **PT3 (concluído, sem commit — nenhum ajuste necessário)**: build produção + lint + type-check verdes. Validação visual feita via Playwright headless (não há navegador interativo neste ambiente): screenshots full-page dos 3 temas na home e nas páginas de blog. Achado durante a validação: seções com animação `whileInView` do Framer Motion (About, timeline de Experience, Skills) apareciam vazias (`opacity:0`) em screenshots automatizados sem scroll real — não é regressão, é o IntersectionObserver não disparando em scroll sintético rápido; resolvido rolando por âncora de seção com espera antes de cada screenshot. Confirmado por inspeção de DOM (classes exatas do Card do DS presentes: `rounded-lg border bg-surface-default border-border-default shadow-sm p-6`) e por opacidade computada subindo com o tempo real de animação. As 3 temas renderizam de forma coerente (cores de ação, texto, superfícies trocando corretamente) na home completa e nas páginas de blog/post individual.
- **Nota de escopo**: rodar `npm run format` sem escopo (`prettier --write .`) reformatou ~24 arquivos fora do que foi tocado nesta sessão (CI workflow, configs, outras seções). Revertidos via `git checkout --` antes do commit do PT2, mantendo só os arquivos realmente alterados.
- **Ainda bloqueado:** PT5–PT6 (analytics) dependem do T30 do tf.ds (integração do analytics nos componentes, Phase 7 de lá, ainda não executada)
- **⚠️ Próximo passo — PT4 é checkpoint do usuário:** deploy preview do portfolio na Vercel (branch `feat/tfds-v2-migration`), validar que builda sem estratégia especial e que os 3 temas estão OK na URL. Ação manual — não tentar fazer deploy sozinho.
- **Spec de referência:** `../tf.ds/.specs/features/tfds-v2/spec.md`
- **Consolidação de branches (2026-07-12):** `feat/tfds-v2-migration` mergeada em `develop` (gate verde: type-check, lint, build) e `develop` fast-forwardado em `main`; ambos pushados para `origin`. `develop` estava 40 commits e `main` 15 commits à frente do remoto havia tempo (incluindo o fix de CVE de segurança do React Server Components) — tudo sincronizado agora. Removidas: `feat/tfds-v2-migration` (local) e as remotas `tfds/button`, `tfds/migration`, `vercel/react-server-components-cve-vu-95hepz` (já eram ancestrais de `develop`). PT4 (deploy preview na Vercel) segue como checkpoint pendente do usuário, agora a validar diretamente em `main`/`develop` já que a branch de feature não existe mais.
